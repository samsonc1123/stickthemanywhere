	1.	Stale/cached bundle being served. Static deployments on Replit sometimes serve an old build if the CDN cache or the browser cache didn’t get busted. If an older index-*.js (from before Convex was wired up, or before a bug fix) is what’s actually loading on the live site, everything you “confirmed via grep” in your local dist folder is irrelevant — it’s not what’s being served. Check: open the live site in an incognito window, DevTools → Network → reload → click the loaded JS file → confirm its hash/filename matches your latest client/dist/assets build exactly.
	2.	Content-Security-Policy blocking connect-src. If there’s any CSP header (added by Replit’s static host defaults, a meta tag, or something left over) that doesn’t explicitly allow wss://gregarious-porpoise-625.convex.cloud, the browser will silently block the WebSocket and Convex will just hang at “connecting” forever — which matches your symptom exactly (getIdentity never resolves, indicator shows OFF). Check: DevTools → Console on the live site, look for a red line like “Refused to connect to ‘wss://…’ because it violates the following Content Security Policy directive.” This is the single most common cause of “works in dev, silently dead in prod” for WebSocket clients.
	3.	The WS connection is attempted but something else fails first. DevTools → Network → filter by “WS” → reload the live page. You want to see: is a connection to convex.cloud even attempted?
	•	No attempt at all → a JS error earlier in page load is preventing that code from running (check Console for a red error thrown before the Convex client ever initializes — could be an unrelated import/runtime error that only shows up in the production bundle due to minification differences).
	•	Attempt shown, but it closes immediately → click it, check the “Messages” and status. A close code will tell you a lot (e.g., 1006 = abnormal closure, often a proxy/CSP/network issue; 4000-series Convex-specific codes usually mean an actual protocol-level rejection).
	4.	The Convex: OFF indicator may be measuring the wrong thing. You mentioned it’s driven by useQuery(api.authUtils.getIdentity) !== undefined. If nobody’s logged in on the admin page, getIdentity can legitimately return null (not undefined) once resolved — but your check !== undefined would still show OFF forever if the query simply hasn’t resolved for connection reasons, or if it resolves to null and your OFF logic doesn’t handle that case. Worth checking this in isolation from the dropdown bug — they might not be the same root cause.

Suggested order of operations:

	1.	Confirm the live bundle matches your latest build (rule out stale cache — this alone fixes a surprising number of “identical code, different behavior” bugs).
	2.	Open Console + Network→WS on the live site and just look — don’t guess, the browser will tell you directly which of the above it is within about 30 seconds.
	3.	Report back what you see (CSP violation? no attempt? closed connection with a code?) and I can narrow the fix immediately.

Want me to also sanity-check your main.tsx/provider setup for anything that behaves differently in a production Vite build vs dev (e.g. env var inlining, provider ordering)?